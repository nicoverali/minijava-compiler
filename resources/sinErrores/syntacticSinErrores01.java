class Persona extends SerVivo {
    public String a, b, c;
    private int d;

    static void main(){}

    static void metodo1(){

    }

    dynamic boolean metodo2(String param1){

    }

    static int metodo3(int param1, String param2) {
        { // Bloque
            ; // Sentencia vacio
            (Casting) system.out.println = 4; // Asignacion con acceso complejo
            (Casting) new Constructor().a = 4; // Asignacion con acceso complejo
            (Casting) this.b = 4; // Asignacion con acceso complejo
            (Casting) (a + b) = 4; // Asignacion con acceso complejo
            (Casting) metodo1() = 4; // Asignacion con acceso complejo
            a = 3; // Asignacion con acceso simple

            system.out.println; // Llamada con acceso complejo
            new Constructor().a; // Llamada con acceso complejo
            this.b; // Llamada con acceso complejo
            (a + b); // Llamada con acceso complejo
            metodo1(); // Llamada con acceso complejo

            int a; // Var local simple
            String b = ((a + b + system.a - (Casting) b * 3) * b); // Var local complejo

            return a; // Return simple
            return system.out.println(); // Return encadenado
            return; // Return vacio
            return ((Casting) a.b.c); // Return expresion

            if (system.out.println('a', "bb") + a + b + (Casting) algo) {
                return a;
            }

            if (true) {
            } else {
                ("Hola");
            }

            if (true) ;
            else if (false)

            for (String b = ((a + b + system.a - (Casting) b * 3) * b); ((Casting) a.b.c); (Casting) new Constructor().a = 4){}
        }

        {} // Bloque vacio
    }
}

class Vacio {

}

class SerVivo{}